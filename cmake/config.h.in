#ifndef CONFIG_JMBDE_MODELS_H
#define CONFIG_JMBDE_MODELS_H

// CMake-Definitionen
#cmakedefine JMBDE_MODELS_INSTALL_QT_LAYOUT
#define JMBDE_MODELS_DEBUG_POSTFIX "${CMAKE_DEBUG_POSTFIX}"

// Relative Installationsverzeichnisse
#define JMBDE_MODELS_PLUGIN_INSTALL_DIR "${PLUGIN_INSTALL_DIR}"
#define JMBDE_MODELS_LIBEXEC_INSTALL_DIR "${LIBEXEC_INSTALL_DIR}"
#define JMBDE_MODELS_BUNDLE_INSTALL_DIR "${BUNDLE_INSTALL_DIR_PRIVATE}"
#define JMBDE_MODELS_BIN_INSTALL_DIR "${BIN_INSTALL_DIR}"
#define JMBDE_MODELS_PROBE_INSTALL_DIR "${PROBE_INSTALL_DIR}"
#define JMBDE_MODELS_TRANSLATION_INSTALL_DIR "${TRANSLATION_INSTALL_DIR}"
#define JMBDE_MODELS_QCH_INSTALL_DIR "${QCH_INSTALL_DIR}"
#define JMBDE_MODELS_TARGET_PLUGIN_INSTALL_DIR "${TARGET_PLUGIN_INSTALL_DIR}"

// Plugin- und ABI-Versionen
#define JMBDE_MODELS_PLUGIN_VERSION "${JMBDE_MODELS_PLUGIN_VERSION}"
#define JMBDE_MODELS_PROBE_ABI "${JMBDE_MODELS_PROBE_ABI}${JMBDE_MODELS_PROBE_ABI_POSTFIX}"

// Relative inverse Installationsverzeichnisse
#define JMBDE_MODELS_INVERSE_BUNDLE_DIR "${JMBDE_MODELS_INVERSE_BUNDLE_DIR}"
#define JMBDE_MODELS_INVERSE_BIN_DIR "${JMBDE_MODELS_INVERSE_BIN_DIR}"
#define JMBDE_MODELS_INVERSE_LIB_DIR "${JMBDE_MODELS_INVERSE_LIB_DIR}"
#define JMBDE_MODELS_INVERSE_PROBE_DIR "${JMBDE_MODELS_INVERSE_PROBE_DIR}"
#define JMBDE_MODELS_INVERSE_LIBEXEC_DIR "${JMBDE_MODELS_INVERSE_LIBEXEC_DIR}"

// Probe-Name
#define APP_PROBE_BASENAME "${PROBE_BASENAME}"

// Standard-Netzwerk-Eigenschaften
#define JMBDE_MODELS_DEFAULT_LOCAL_ADDRESS "127.0.0.1"
#define JMBDE_MODELS_DEFAULT_LOCAL_TCP_URL "tcp://127.0.0.1"
#define JMBDE_MODELS_DEFAULT_ANY_ADDRESS "0.0.0.0"
#define JMBDE_MODELS_DEFAULT_ANY_TCP_URL "tcp://0.0.0.0"

// Build-Optionen
#cmakedefine HAVE_STDINT_H
#cmakedefine HAVE_BACKTRACE
#cmakedefine HAVE_CXA_DEMANGLE

#cmakedefine HAVE_QT_WIDGETS
#cmakedefine HAVE_QT_SCXML
#cmakedefine HAVE_QT_SVG
#cmakedefine HAVE_QT_DESIGNER
#cmakedefine HAVE_QT_PRINTSUPPORT

#cmakedefine HAVE_SYNTAX_HIGHLIGHTING

#cmakedefine HAVE_ELF_H
#cmakedefine HAVE_SYS_ELF_H
#cmakedefine HAVE_ELF

#cmakedefine JMBDE_MODELS_ENABLE_GPL_ONLY_FEATURES
#cmakedefine JMBDE_MODELS_CORE_ONLY_LAUNCHER
#cmakedefine JMBDE_MODELS_STATIC_PROBE
#cmakedefine JMBDE_MODELS_DISABLE_FEEDBACK

#cmakedefine JMBDE_MODELS_WITH_WIDGET3D

#define QT_LIBINFIX "@QT_LIBINFIX@"

#endif // CONFIG_JMBDE_MODELS_H
